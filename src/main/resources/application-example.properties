# ============================================================
# This file serves as a template for your application's configuration.
#
# Steps to Get Started:
# 1. Make a copy of this file and rename it to `application.properties`.
# 2. Replace the placeholder values below with your actual configuration.
# 4. Use schema.sql and data.sql to create and populate your schemas
# 3. Ensure `application.properties` is excluded from version control
#    (check the `.gitignore` file to confirm).
#
# Remember: Never commit sensitive information (like passwords or API keys)
# to your repository.
# ============================================================

spring.application.name=students management system

# Database settings (example)
spring.datasource.url=jdbc:postgresql://localhost:5432/your_database
spring.datasource.username=your_username
spring.datasource.password=your_password

# Hibernate (JPA) settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true